# Payload Block Settings

Scaffold package for block-related Payload helpers and plugins.

## Intended use

This package is the starting point for helpers that standardize block settings across Payload projects.

## Structure

- `src/` contains the published source.
- `dev/` contains a local Payload app for iteration and manual testing.
