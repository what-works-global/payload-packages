'use client'
// Vendored from payload@3.84.1 `packages/next/src/views/Versions/cells/ID/index.tsx` (MIT).
import React, { Fragment } from 'react'

export function IDCell({ id }: { id: number | string }) {
  return <Fragment>{id}</Fragment>
}
